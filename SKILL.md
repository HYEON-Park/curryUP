# 채용 공고 수집 대상 사이트

매일 00:00~09:29 사이 아래 목록에 등록된 URL에서 채용 공고를 수집합니다.
한 줄에 하나의 URL을 `-` 뒤에 입력하세요. `#`으로 시작하는 줄은 비활성(주석) 처리됩니다.
- https://www.saramin.co.kr/zf_user/search/recruit?searchType=search&searchword=백엔드+개발
- https://www.saramin.co.kr/zf_user/search/recruit?searchType=search&searchword=풀스택+개발
- https://www.jobkorea.co.kr/Search/?stext=백엔드+개발
- https://www.jobkorea.co.kr/Search/?stext=풀스택+개발

# 마감 임박 공고 자동 삭제

공고 수집 후 대시보드에서 아래 조건에 해당하는 공고를 삭제한다.
(D-5까지 마감임박 공고는 수집 단계에서 모두 삭제한다.)

- 당일 마감 (D-0)
- D-1 (내일 마감)
- D-2 (모레 마감)
- D-3 (3일 뒤 마감)
- D-4 (4일 뒤 마감)
- D-5 (5일 뒤 마감)

# SKILL.md 확인 루틴

## 대상 파일
- 경로: `C:\R\SKILL.md`

## 확인 시점 (Trigger)
Claude Code는 아래 시점에 반드시 `C:\R\SKILL.md`를 읽어 최신 내용을 반영한다.

1. **UPDATE 버튼 클릭 시**
   - 위치: `dashboard-toolbar-top` 내 `<button>UPD****ATE</button>`
   - 사용자가 클릭하면 즉시 `C:\R\SKILL.md`를 재적재한다.

2. **서버 기동 시**
   - 애플리케이션 최초 부트스트랩 단계에서 `C:\R\SKILL.md`를 읽는다.

3. **서버 재기동 시**
   - 서버 재기동 시에도 기동 시점과 동일한 로직으로 `C:\R\SKILL.md`를 재적재한다.

## 처리 순서
1. `C:\R\SKILL.md` 파일 존재 여부 확인
2. 존재 시 → 내용 읽기 및 파싱
3. 스킬 레지스트리(메모리 캐시)에 반영 (기존 캐시가 있으면 덮어쓰기)
4. 처리 결과를 로그로 기록

## 예외 처리
- 파일이 존재하지 않는 경우 → 경고 로그를 남기고 기존 캐시를 유지한다.
- 파싱 실패 시 → 오류 로그를 남기고 기존 캐시를 유지한다. UPDATE 트리거인 경우 사용자에게 실패 메시지를 노출한다.
- API 호출 실패(UPDATE 버튼) → 사용자에게 실패 원인 메시지를 노출하고 재시도를 안내한다.

## 로깅 항목
- 트리거 구분 (UPDATE / STARTUP / RESTART)
- 실행 시각
- 대상 파일 경로 (`C:\R\SKILL.md`)
- 처리 결과 (성공 / 실패)
- 실패 시 오류 메시지

# AI 프롬프트 생성 조건

## 대상 공고 필터링 규칙
AI 프롬프트 생성 대상 공고는 아래 조건을 **모두 만족**해야 한다.

1. **대시보드 게시 상태**
   - 대시보드에 정상적으로 게시되어 있는 공고

2. **마감 임박 제외 (D-0 ~ D-2 제외)**
   - 마감까지 잔여 기간이 3일 이상인 공고만 대상
   - D-Day 계산 기준: 공고 마감일 - 현재 일자
   - D-0(마감 당일), D-1(마감 하루 전), D-2(마감 이틀 전) 공고는 제외
   - 마감일 정보가 누락된 공고는 제외 후 경고 로그 기록

3. **삭제 공고 제외**
   - 논리 삭제 job-card-delete 플래그가 설정된 공고 제외

4. **AI 문서 미생성 공고**
   - 자기소개서(coverLetter) 및 경력기술서(workExperience)가 하나도 생성되지 않은 공고만 대상
   - 하나라도 생성된 경우 대상에서 제외

## 처리 흐름
1. 대시보드에 게시된 전체 공고 조회
2. 마감 임박(D-0~2) 필터 적용
3. 삭제 공고 필터 적용
4. AI 문서 미생성 필터 적용
5. 최종 결과 공고들 -> AI 프롬프트 생성 대상 리스트로 반환

## 예외 처리
- 필터링 결과 0건 → "생성 대상 공고 없음" 안내 메시지 노출
- 대상 공고 조회 실패 → 오류 로그 기록 후 사용자에게 재시도 안내

## 로깅 항목
- 실행 시각
- 전체 공고 수
- 조건별 잔여 건수 (마감 임박 필터 후 / 삭제 필터 후 / 문서 미생성 필터 후)
- 최종 생성 대상 건수
